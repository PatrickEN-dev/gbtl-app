// src/services/notifications.ts
// Expo push notifications are temporarily disabled for Expo Go.
// Re-enable only when migrating to a development build or standalone app.

export async function registerForPushNotificationsAsync(): Promise<string | null> {
  return null;
}
